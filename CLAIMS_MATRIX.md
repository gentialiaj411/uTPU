# CLAIMS_MATRIX.md

| Claim | Current evidence | Risk level | Missing proof | Suggested verification |
|---|---|---|---|---|
| Pass-based Graph IR pipeline exists between FX import and lowering | `graph_passes.py`, `pytorch_compiler.py`, `test_graph_passes.py` | Low | None for subset | Keep pass tests in regression |
| Deterministic NumPy reference interpreter exists | `graph_reference_interpreter.py`, `test_reference_interpreter.py` | Low | None for subset ops | Add more op-boundary tests if scope grows |
| Deliverable B execution is currently validated | `python -m pytest firmware/host/test_reference_interpreter.py -q` run on 2026-05-17 (`2 passed`) | Low | Wider op-surface coverage beyond current subset | Add additional tests if new IR ops are added |
| Differential backend-vs-reference harness exists with artifact output | `differential_test_harness.py`, `test_differential_harness.py`, `build/reports/differential_test_report.json` | Low | None for current harness contract | Keep report schema stable in tests |
| Deliverable C execution is currently validated | `python -m pytest firmware/host/test_differential_harness.py -q` run on 2026-05-18 (`1 passed`), refreshed `build/reports/differential_test_report.json` | Low | Report schema lock not yet enforced | Add JSON schema-lock test for artifact contract |
| Differential fixtures are non-identity (not pure passthrough) | seeded sparse signed weights/inputs plus tiny deterministic jitter in `differential_test_harness.py`; report max_abs_error is nonzero (`1.19e-07` to `3.49e-07`) and within tolerance | Low | None for fixture intent | Keep fixture logic stable and documented |
| TorchInductor oracle is wired into differential testing | `differential_test_harness.py` adds `torch_compile` backend using `torch.compile(..., backend="inductor", fullgraph=True)`; `test_differential_harness.py` requires the oracle entry | Medium | Current Windows run skips with `WinError 50`, so this is not passing TorchInductor evidence yet | Rerun on Linux/WSL with working TorchInductor before resume wording says validated against TorchInductor output |
| CUDA blocked-FC latency cost model has calibrated CTA-working-set form | `firmware/host/cost_model.py`, `firmware/host/calibrate_cost_model.py`, current `build/reports/cost_model_calibration.json` (`refit_from_existing_measurements=true`) with log_R2 `0.9360`, MAPE `11.85%`, p95 `31.16%` | Low for measured-grid interpolation; Medium for unseen-shape prediction | Strict actual-layer-shape holdout has p95 `37.85%` and structured `(in=64,out=512,tpb=256)` residuals | Keep claim scoped to calibrated/measured shape-schedule grid unless strict residual is explained |
| Cost-model fix explains prior `(in=512,out=512,tpb=32,u=1)` failure cluster | Fresh artifact shows target cluster near measured latency; requested triplet holdout test log_R2 `0.9283`, MAPE `10.11%`, p95 `23.42%` | Low | None for the original failure cluster | Use kernel-code explanation plus fresh calibration artifact |
| Cost-model-pruned autotuner reduces profiled schedule candidates with bounded quality loss | `cuda_autotuner.py` supports policy pruning; current replay reports policy max regression `2.995%`, within-1% fraction `0.9167`, avg profiled candidates `4.875` of 16, search reduction `3.282x` | Low for measured-data replay; Medium for live tuning wall-clock | Live top-K-only CUDA tuning wall-clock reduction not separately timed | Optionally run live pruned vs exhaustive tuning on a subset |
| Cost model/autotuner ranking is now schedule-aware (including unroll-sensitive behavior) | `firmware/host/cost_model.py` adds schedule-aware analytical terms; `firmware/host/test_cuda_autotuner.py` includes schedule-aware ranking and pruning fallback/diversity tests (`18 passed`) | Medium because exact top-1 ranking remains weak | Single-winner prediction is not reliable; policy pruning is the supported path | Track top-1, top-k, within-1%, and policy-vs-strict replay metrics after each refit |
| Calibration/replay now reflects the extended schedule-aware coefficient basis | `calibrate_cost_model.py` refit mode recomputes the current feature basis from existing measured rows; current artifact records pairwise objective metadata and reports log_R2 `0.9204`, MAPE `12.85%`, p95 `35.09%` | Medium | Exact top-1 ranking remains weak | Keep claims scoped to pruning quality rather than single-winner prediction |
| Pruned replay now matches production pruning policy semantics | `firmware/host/evaluate_pruned_autotuner.py` uses `select_pruned_candidates()`; current replay reports policy vs strict metrics separately, with policy avg profiled candidates `4.875`, search reduction `3.282x`, containment `0.7917`, max regression `2.995%`, and within-1% fraction `0.9167` | Low | None for report-honesty claim | Keep policy/strict dual metrics in artifact schema and tests |
| Calibration now exposes per-shape ranking diagnostics for schedule selection quality | `firmware/host/calibrate_cost_model.py` adds top-1/top-k ranking diagnostics, per-shape Spearman rho, and pairwise ordering accuracy; current calibration JSON/MD include these fields | Low | None for diagnostic availability | Use diagnostics to avoid claiming single-winner prediction while top-1 remains weak |
| Calibration can be refit from existing measurements without new CUDA timing | `firmware/host/calibrate_cost_model.py` supports `--refit-from-existing-json`; current refit artifact marks `refit_from_existing_measurements=true` and updates coefficients/metrics | Low | None for feature existence; objective quality still limited | Use refit mode for lambda/scale sweeps before running expensive CUDA timing |
| Cost-model coefficient fitting includes a pairwise ordering objective | `_fit_coefficients()` in `firmware/host/calibrate_cost_model.py` blends normalized log-latency residuals with filtered within-shape schedule-median pairwise ordering; current refit has log_R2 `0.9204`, MAPE `12.85%`, p95 `35.09%`; focused tests pass (`18 passed`) | Medium | Aggregate latency fit is below original 5-term artifact and exact top-1 ranking remains weak | Tune pruning safety policy if more search reduction is needed; keep claims scoped to bounded replay quality |
| CUDA cost model holds median absolute percent error within 8.98% across 24 frozen autotuner shapes | `firmware/host/cost_model.py`, `firmware/host/calibrate_cost_model.py`, `firmware/host/cost_model_regression.py`, `firmware/host/test_cost_model_regression.py`, `bench/results/cost_model_regression.json`; frozen shapes from `calibrate_cost_model.py::_shape_grid()` layer pairs; baseline median abs error `8.98%`, p95 `15.71%`, regression ceiling `15.71%` (baseline + `6.73` pp tolerance); refit after `large_out_small_k_wave_tpb_efficiency` term added (`build/reports/cost_model_calibration.json` log_R2 `0.9360`, MAPE `11.85%`); CI replay gate passes | Verified | Live CUDA remeasurement not required for CI gate; `(in=64,out=512,tpb=256)` cluster still has structured overprediction | Regenerate `bench/results/cost_model_regression.json` after intentional cost-model or calibration changes |
| ResNet-18 compiles end-to-end on the CUDA graph backend with eager PyTorch and TorchInductor parity at `rtol=1e-3`, `atol=1e-3` | `firmware/host/fx_importer.py`, `firmware/host/graph_conv_ops.py`, `firmware/host/graph_passes.py` (`conv_bn_fusion`), `firmware/host/graph_reference_interpreter.py`, `firmware/host/cuda_blocked_fc_backend.py`, `firmware/host/run_real_model_end_to_end.py`, `firmware/host/inductor_oracle_subprocess.py`, `firmware/host/test_real_model_ops.py`, `firmware/host/test_real_model_end_to_end.py`, `bench/results/real_model_end_to_end.json` (2026-05-24 WSL2: `cuda_available=true`, `execution_backend=cuda_graph_executor`, all 3 seeds pass eager + Inductor; host PyTorch `2.11.0+cu130`, CUDA `13.0`, GPU `NVIDIA GeForce RTX 5070 Laptop GPU`, Python `3.14.4`) | Verified | uTPU ISA does not execute full ResNet; Inductor oracle runs in an isolated subprocess to avoid NVRTC driver-context clashes | Re-run `python firmware/host/run_real_model_end_to_end.py` on WSL2/Linux with CUDA + gcc after ResNet graph or harness changes |
| Compiler lowers fused MLPs to 2-PE schedule, bit-identical to 1-PE output (simulator) | `firmware/host/lowering_fused_mlp_utpu.py`, `firmware/host/isa_simulator.py`, `firmware/host/test_multi_pe_sim.py`, `bench/results/multi_pe_sim.json`; `num_pe=2` K-split FC1 schedule for `case2_multi_k`; 1-PE and 2-PE fetch bytes match NumPy reference; parallel cycle estimate `167` vs 1-PE sequential `220` (`24.1%` reduction) with `7.7%` sequential overhead | Verified | Simulator-only; current RTL is single-PE; case1 single-K falls back to 1-PE schedule | Keep `test_multi_pe_sim.py` in CI; do not claim FPGA speedup |
| Memory planning with liveness-driven buffer reuse exists | `memory_planning_pass` in `graph_passes.py`, runtime plans carry `memory_plan`, `test_graph_passes.py`, `build/reports/memory_plan_report.json` | Low for pass behavior | Reduction evidence is on a liveness sample graph, not an end-to-end model benchmark | Run on larger supported graphs if available |
| Python ISA simulator bitmatches Verilog RTL on compiled fused MLP programs | `firmware/host/isa_simulator.py`, `firmware/host/run_isa_rtl_bitmatch.py`, `firmware/host/test_isa_simulator.py`, `firmware/host/test_rtl_sim_artifact.py`, `build/reports/isa_rtl_bitmatch_report.json`; current artifact has `all_isa_expected_bitmatch=true`, `all_isa_rtl_bitmatch=true` for 3 deterministic fused cases and full output tensors | Low for current fused-program path | Only the fused MLP path is covered; no board execution | Increase N and shape coverage if expanding the resume claim |
| Residual-add RUN op is implemented and ISA-validated for the fused residual MLP path; RTL bitmatch is still pending on the current artifact | `firmware/host/isa_encoder.py`, `firmware/host/isa_simulator.py`, `firmware/host/program_loader.py`, `firmware/host/lowering_fused_mlp_utpu.py`, `firmware/host/run_isa_rtl_residual_bitmatch.py`, `firmware/host/test_rtl_residual_sim_artifact.py`, `rtl/top/top.sv`, `rtl/tb/tb_fused_residual_program.sv`, `build/reports/isa_rtl_residual_bitmatch_report.json`; current artifact has `all_isa_expected_bitmatch=true`, `rtl_sim_executed=false`, `all_isa_rtl_bitmatch=false`, and per-case `isa_rtl_bitmatch=null` for the deterministic residual case | Medium for the current single-case path | RTL bitmatch is still missing on the current artifact; no board execution and residual mode remains default-off for legacy programs | Re-run `python firmware/host/run_isa_rtl_residual_bitmatch.py` on an iverilog-capable host before restoring stronger RTL wording |
| 8x8 MNIST quantized MLP end-to-end demo bit-matches RTL in iverilog simulation and stays bit-exact vs the **raw-integer reference** for the lowered program (not the scaled PyTorch quantized model) | `firmware/host/run_mnist_utpu_demo.py`, `firmware/host/test_mnist_utpu_demo.py`, `firmware/host/program_loader.py`, `firmware/host/lowering_fused_mlp_utpu.py`, `rtl/tb/tb_mnist_utpu_program.sv`, `build/reports/mnist_utpu_demo.json`; current artifact has `rtl_sim_executed=true`, `rtl_sim_passed=true`, `isa_rtl_bitmatch=true`, `bit_exact_vs_reference=true`, `reference_semantics="raw_integer_reference_for_lowered_fused_mlp_program"`, `float_acc=0.9289`, diagnostic `proxy_quant_acc=0.9188` (legacy field `quant_acc` retained), deployed `deployed_raw_integer_acc=0.3500`, `fits_instruction_bram=true` on `pynqz2_bram_max`. The bitmatch chain is **raw-integer reference → ISA sim → RTL**; the three showcased argmax cases (`label7/2/1`) match the proxy-scaled PyTorch path but **not** deployed raw-integer semantics. Accelerator-useful accuracy is **invalid until per-layer requantization lands** — `0.9188` is proxy-scale evidence only. | Low for the fixed demo path (correctness); **High** if `quant_acc`/`proxy_quant_acc` is cited as deployed accelerator accuracy | Simulation + RTL only; no FPGA execution claim; locally reproduced rather than CI-gated (CI skips when the JSON is absent on a fresh checkout); **proxy vs deployed accuracy split must stay explicit** — the hardware datapath applies no per-layer dequant/requant scales | Keep the JSON lock and host test in CI; do not promote this to a board-execution claim or a useful-quantized-accuracy claim until requantization is implemented and re-measured under deployed semantics |
| uTPU blocked-FC path executes batched GEMM on the ISA simulator and Verilog RTL simulation, stays bit-exact vs a NumPy oracle, and keeps B=1 vector lowering byte-identical to the pre-batching dc7a517 vector path (vector and 1-row matrix activations emit identical programs) | `firmware/host/lowering_blocked_fc_utpu.py` adds opt-in batched lowering for matrix-shaped activations, `firmware/host/isa_encoder.py` carries batch count in extended-mode metadata, `firmware/host/isa_simulator.py` executes batched accumulation/finalize while preserving the legacy vector path, `firmware/host/run_batched_gemm_correctness.py`, `firmware/host/test_batched_gemm.py` (`test_batched_gemm_b1_program_matches_pre_batching_legacy_bytes` pins B=1 against `firmware/host/fixtures/b1_legacy_program_bytes.json` sourced from dc7a517; `test_batched_gemm_b1_vector_and_singleton_matrix_emit_identical_programs` checks vector vs 1-row matrix only), `firmware/host/test_batched_gemm_rtl_artifact.py`, `firmware/host/test_batched_gemm_rtl_sweep.py`, `firmware/host/run_rtl_batched_gemm_sim.py`, `firmware/host/run_rtl_batched_gemm_sweep.py`, `rtl/tb/tb_batched_gemm.sv`, `bench/results/batched_gemm_correctness.json`, `build/reports/rtl_batched_gemm_sweep.json`; host artifact sweeps 7 `(shape, B)` cases, reports `all_cases_bit_exact_vs_oracle=true`, `all_cases_deterministic=true`, `all_b1_programs_byte_identical=true` (vector vs singleton-matrix within-run), and non-increasing `weight_load_fraction_of_total_cycles` as `B` grows (e.g. `(16,16)` falls from `0.01235` at `B=1` to `0.00606` at `B=4` to `0.00508` at `B=8`); RTL sweep bit-matches deterministic `(16,16,B=1/4/16/32/64)`, `(32,32,B=1/16)`, and `(64,64,B=1/16)` cases (`all_cases_passed=true`) | Verified (simulation) | Current evidence is the shipping INT4 datapath plus extended-address batched metadata; this is ISA-sim + iverilog RTL evidence only, not board execution. Behavioral lowering changes that alter emitted program bytes require their own commit + correctness regen + claims re-check, not a CI-only commit. | Re-run `python firmware/host/run_batched_gemm_correctness.py` and `python -m pytest firmware/host/test_batched_gemm.py firmware/host/test_batched_gemm_rtl_artifact.py firmware/host/test_batched_gemm_rtl_sweep.py -q` after any change to blocked-FC lowering, ISA encoding, or `rtl/top/top.sv`; keep B=1 vector bytes pinned to the dc7a517 fixture unless a future phase intentionally changes the ISA |
| RTL perf-counter characterization now shows a real streamed systolic result on the single-tile flagship, plus a narrower multi-tile improvement claim measured with an honest compute-span duty metric: for the 16x16 blocked-FC tile, busy cycles grow sublinearly with batch and PE occupancy climbs toward the streaming ceiling `B / (2N + B)`; for 32x32 and 64x64, hoisting tile payloads out of the inner loop materially improves compute-span duty versus the serialized baseline, but does not solve the remaining load-to-array gaps | `firmware/host/run_systolic_characterization.py`, `firmware/host/test_systolic_characterization.py`, `bench/results/systolic_characterization.json`; artifact reports three RTL-measured metrics per `(shape,B)` case: `pe_occupancy = useful_macs / (ARRAY_SIZE^2 * rtl_busy_counter)`, `busy_fraction = rtl_busy_counter / rtl_cycle_counter`, and `compute_span_duty_cycle = compute_busy_cycles / compute_span_cycles` where the span runs from first accumulate start to last accumulate done. Current committed flagship curve is the single-tile `(16,16)` family at `B=1/4/16/32/64`: `rtl_busy_counter = 38/43/55/71/103`, marginal busy cycles per added batch element `1.67/1.00/1.00/1.00`, and `pe_occupancy = 0.0263/0.0930/0.2909/0.4507/0.6214`, which tracks the streaming ceiling `0.0303/0.1111/0.3333/0.5000/0.6667` closely enough to serve as the measured flagship. The same artifact now reports multi-tile before/after duty-cycle comparisons under hoisted payloads: `(32,32,B=4/16/32)` `0.0562/0.0406/0.0334 -> 0.2935/0.1391/0.0985`; `(64,64,B=4/16/32)` `0.0480/0.0365/0.0307 -> 0.3343/0.1736/0.1255`. | Benchmarked | The flagship claim remains anchored by the single-tile 16x16 family. Multi-tile wording must stay narrow: the improved rows remove serialized BRAM-to-buffer refill from the inner loop, but compute-span duty still falls well below the single-tile case because `LOAD_STATE` gaps remain serialized and grow with batch width. `rtl_busy_counter` remains a busy-window metric, not a whole-orchestration wall-clock metric; `rtl_cycle_counter` still carries the remaining serialized control cost separately. Simulation-only evidence; no board execution claim. Deferred CI note: the batching/streaming GitHub Actions gate is still pending manual confirmation and must not be cited as green proof yet. | Re-run `python firmware/host/run_systolic_characterization.py` and `python -m pytest firmware/host/test_systolic_characterization.py -q` after any change to `rtl/top/top.sv`, `rtl/PEArray/*`, or the blocked-FC lowering; keep the analytical model secondary, preserve the single-tile sublinear-busy regression gates, and do not strengthen the multi-tile claim beyond the before/after compute-span-duty improvement until the remaining load gaps are reduced |
| Deployed INT8 accuracy **97.33%** (float **97.30%**) under the hardware-actual integer contract: int matmul ? per-layer fixed-point requant (multiply ? arithmetic-right-shift-truncation ? saturate) in finalize, no float scales applied at inference. Corroborated bit-exact between the host integer reference, the ISA simulator, and iverilog RTL on representative shapes and the FC2 layer; FC1 (52k-word program) is ISA-validated only, not RTL-executed. INT4 reported honestly at **58.32%**. Simulation + host-reference only ? not a full-network RTL execution claim | `firmware/host/requantization.py`, `firmware/host/isa_encoder.py` (`encodeRequantParams`), `firmware/host/isa_simulator.py` (finalize requant), `firmware/host/lowering_blocked_fc_utpu.py`, `rtl/quantizer/quantizer.sv`, `rtl/quantizer/quantizer_array.sv`, `rtl/top/top.sv`, `rtl/tb/tb_batched_gemm.sv`, `firmware/host/run_real_model_accelerator.py`, `firmware/host/test_blocked_fc_requant.py`, `firmware/host/test_real_model_accelerator.py`, `firmware/host/test_batched_gemm.py`, `build/reports/requant_rtl_o16_i16_b4.json`, `bench/results/real_model_accelerator.json`; artifact `model_name="mnist_14x14_196x256x10_mlp_ptq"`, `float_accuracy=0.973`, deployed `int8_accuracy=0.9733`, deployed `int4_accuracy=0.5832`, `quantization_contract.rounding_mode="arithmetic_right_shift_truncation"`, `bit_exact_vs_reference=true`, `isa_bit_exact_vs_reference=true`, `isa_rtl_bitmatch=true` on labeled `B=4` subset; FC2 RTL executed (`layer_program_words.fc2=4003`), FC1 ISA-only (`layer_program_words.fc1=52225`, RTL skipped above 12k-word threshold); no-X proof recorded in `build/reports/requant_rtl_o16_i16_b4.json` (`[ OK ] No X on quantizer finalize outputs`) | Verified (simulation) | **Four-level status:** simulated (integer reference ? ISA sim ? representative RTL bitmatch), CI-validated (requant + blocked-FC host tests), synthesized at 50 MHz on Artix-7 A7-100T (`8x8` INT8, `MAX_BATCH_COUNT=4`, post-route `WNS=+0.416 ns`, `DSP=192/240`, `.bit` generated; see `bench/results/baseline_8x8_current_rtl_synth.json`), hardware-executed pending (no on-silicon board run staged yet). **Provenance note:** the older p4_2 `100 MHz`, `64 DSP`, `WNS=+1.163 ns` artifact predates batching and is not current-RTL timing evidence. FC1 not RTL-executed. INT4 at 58.32% is honest, not a headline. Deferred CI: requant + capstone tests not yet in `.github/workflows/ci.yml`. | Re-run `python firmware/host/run_real_model_accelerator.py`, `python -m pytest firmware/host/test_blocked_fc_requant.py firmware/host/test_real_model_accelerator.py firmware/host/test_batched_gemm.py -q`, and a fresh Vivado batch synth on a Vivado-capable host after any change to the requant contract, `quantizer.sv`, `quantizer_array.sv`, `top.sv`, or blocked-FC lowering |
| Lowered `conv2d` to the accelerator's GEMM datapath via `im2col`; a full-resolution CIFAR-10 all-conv CNN runs end-to-end with deployed INT8 accuracy **69.11%** vs float **70.45%** (**1.34-pt gap**) under the deployed integer contract. Backend parity is bit-exact across an integer reference, the CUDA backend, and the uTPU ISA simulator on a validated **128-image** subset, and RTL is bit-exact on the representative `conv1` layer. Simulation only. | `firmware/host/utpu_conv2d_lowering.py` (`lower_conv2d_im2col_utpu`), `firmware/host/run_real_cnn_accelerator.py`, `firmware/host/test_utpu_conv2d_lowering.py`, `firmware/host/test_real_cnn_accelerator.py`, `bench/results/utpu_conv2d_validation.json`, `bench/results/utpu_small_cnn_validation.json`; artifact `model_name="cifar10_32x32_fullres_small_allconv_int8"`, `mapping.strategy="im2col"`, full-test `float_accuracy=0.7045`, `deployed_int8_accuracy_integer_oracle_full_test=0.6911`, validated subset `size=128`, `oracle_utpu_bit_exact_validated_subset=true`, `oracle_cuda_bit_exact_validated_subset=true`, `cuda_utpu_bit_exact_validated_subset=true`, `backend_parity_validated_subset.backends_skipped=[]`, representative RTL `rtl_representative_results.conv1.rtl_sim_passed=true` | Verified (simulation) | **Scope fence:** full-10k accuracy is scored on the deployed integer reference, not by running all 10k images through the cycle simulator. The integer reference is bit-exact-linked to CUDA and the uTPU ISA simulator on the validated 128-image subset. RTL evidence is representative-layer only (`conv1` passed; `conv4` and `fc` were honestly skipped as oversized). **Forbidden framing:** on-silicon, full-network RTL execution, or "all 10k through the cycle sim". | Re-run `python firmware/host/run_real_cnn_accelerator.py` and `python -m pytest firmware/host/test_utpu_conv2d_lowering.py firmware/host/test_real_cnn_accelerator.py -q` after any change to conv lowering, the integer contract, or the CNN artifact path |
| GitHub front page is evidence-linked and scope-bounded | `README.md`, `docs/EVIDENCE.md`, `docs/inspect_compiler_pipeline_demo.svg` | Low | Raw `build/reports/*` artifacts are ignored and must be regenerated locally | Keep tracked evidence summary current after new benchmark/report runs |
| GitHub Actions CI invokes current host tests | `.github/workflows/ci.yml` now points at existing host tests; `test_footprint_baseline.py` skips when optional local model weights are absent; local mirror command passed with `16 passed`; GitHub Actions run `26059965031` passed on `main` at commit `a2384db` | Low | None for the current narrow CI gate | Keep optional-artifact tests skippable unless artifacts are committed |
| CUDA comparisons skip cleanly when CUDA unavailable | `test_differential_harness.py` skip assertion path | Low | Runtime coverage on non-CUDA machine not run in this session | Re-run on CPU-only environment |
| uTPU backend numerically validated against oracle | Currently via `quantized_reference_emulation` software path plus simulation-only ISA/RTL evidence (`build/reports/isa_rtl_bitmatch_report.json`, `build/reports/isa_rtl_residual_bitmatch_report.json`, `build/reports/mnist_utpu_demo.json`) | Medium | No captured on-board FPGA execution evidence is staged in-repo; P0 remains open | Keep the software + simulation evidence linked, and only promote to board-backed validation after `docs/evidence/fpga_onboard/` artifacts and a replay test land |
| `torch.compile` custom backend dispatches supported FX subgraphs through the uTPU compile pipeline with explicit op-coverage checks and clean eager fallback for unsupported ops | `firmware/host/torch_compile_backend.py`, `firmware/host/test_torch_compile_backend.py` (`5 passed, 1 skipped` local), `build/reports/torch_compile_backend_report.json` (current local: `subgraphs_seen=1`, `subgraphs_fallback=1`, non-empty `unsupported_op_counts`) | Low for current bounded op-set, Medium until Linux/WSL CI run captures full dynamo + CUDA exercise | Linux/WSL CI run id for this backend test; CUDA-capable run proving `subgraphs_compiled > 0` in artifact | Run `.github/workflows/ci.yml` with `test_torch_compile_backend.py` included on Linux runner; capture run id and attach artifact |
| General PyTorch compiler support | Not supported | High | Broad op/model coverage | Keep this claim out of resume/docs |
| Transformer attention/RMSNorm ops run through Graph IR reference and CUDA execution paths | `firmware/host/test_attention_op.py`, `build/reports/attention_op_validation.json` | Low | Broader tensor-shape fuzzing | Add additional shape sweep test cases |
| Persistent KV-cache memory planning is represented in runtime memory metadata | `firmware/host/test_kv_cache_planning.py`, `build/reports/kv_cache_plan_report.json` | Low | Multi-step runtime allocator consumption proof | Integrate kv layout consumption in runtime path |
| Compile-time INT4 group-wise quantization achieves substantial weight reduction with parity bounds | `firmware/host/test_int4_quantization.py`, `build/reports/int4_quantization_report.json` (`71.875%` reduction) | Low | Wider model-layer coverage | Add multi-layer quantization regression test |
| End-to-end transformer decode parity works via handcrafted traceable model fallback | `firmware/host/gpt2_parity_handcrafted.py`, `build/reports/transformer_parity.json` (`6/8` token match with INT4) | Medium | HF GPT-2 FX import on current Windows/Python stack | Re-run GPT-2 import/parity on environment without `co_varnames` tracing limitation |
| Transformer graph ops are runtime-planned/executable in compiled CUDA path (mixed-op path) | `firmware/host/graph_runtime_plan.py`, `firmware/host/compiled_runtime.py`; runtime validation includes attention mask rank-2/3/4 broadcasting checks, batch/head broadcast checks, permute sanity checks, and q/k/v contract checks; focused validation `python -m pytest firmware/host/test_compiled_runtime_validation.py -q` passes in current workspace | Low to Medium | Broader model coverage beyond current block-level tests | Keep transformer integration + importer + runtime validation tests in regression; expand matrix conservatively |
| Affine `LayerNorm`/`RMSNorm` parameters are imported and executed in compiled transformer graph-op path | `firmware/host/fx_importer.py` captures `norm_kind`, `normalized_shape`, `weight`, `bias`; `firmware/host/cuda_blocked_fc_backend.py` executes `layer_norm` vs `rms_norm` formulas and applies affine params; covered by `test_fx_importer.py` and affine case in `test_transformer_integration.py` | Medium | Exact CI evidence for this local delta is pending | Commit/push and capture CI run id before broadening external claims |
| Runtime validation rejects invalid norm shape/affine contracts before compiled execution | `firmware/host/compiled_runtime.py` validates `normalized_shape` compatibility and affine `weight`/`bias` shape matches; `firmware/host/test_compiled_runtime_validation.py` includes explicit failing cases for normalized-shape, weight-shape, and bias-shape mismatches | Low to Medium | CI proof for current uncommitted workspace delta is pending | Keep norm validation tests in CI regression and rerun CI after push |
| Model-level transformer parity matrix test exists, emits artifact, and passes | `firmware/host/test_transformer_integration.py` covers seq `8/16/24/32/48`, multiple hidden/head configs, mask variants (`2D`, `3D`, `4D` head-broadcast, `4D` full), and affine norm coverage for both `RMSNorm` and `LayerNorm`; emits deterministic `build/reports/transformer_parity_report.json`. Current artifact still shows very small observed error (`max_abs_error=5.900859832763672e-06` on the largest recorded case) | Medium | CI gate is currently `max_abs<=3e-3` / `max_rel<=3e-2`; measured `max_abs_error` is ~`5.9e-6` in `build/reports/transformer_parity_report.json`. Tightening the gate to the documented `1e-4` / `3e-3` thresholds is deferred to a host where `test_transformer_integration.py` actually runs; the focused pytest skips on Windows | Re-run the focused transformer test on a host where the cases execute, then tighten the checked-in gate to the documented `1e-4` / `3e-3` thresholds and capture CI proof for the pushed revision |
| CI host regression covers importer/compiler + transformer integration + runtime negative validation tests | `.github/workflows/ci.yml` command includes `test_fx_importer.py`, `test_pytorch_compiler.py`, `test_transformer_integration.py`, and `test_compiled_runtime_validation.py`; latest local CI-equivalent command passed with `24 passed` | Low | Exact CI proof for current uncommitted workspace is missing | Commit/push and verify next CI run for this exact revision |
| CI host regression with transformer test is green on GitHub Actions | GitHub Actions run `26256684852` succeeded on `main` for commit `75a626d7e6064a1239fb26956577c755a8d13aae` with workflow `CI` | Low | None for this revision | Keep tracking latest green run id in context docs after subsequent CI changes |
| CI host regression (latest committed tip) is green on GitHub Actions | GitHub Actions run `26257867510` succeeded on `main` for commit `a83bf447d38fdacc9e4b0724d83172eb9b06d5ff` with workflow `CI` | Low | Uncommitted local changes are outside this run scope | Re-run CI after pushing current workspace changes |
| CI host regression (current pushed tip) is green on GitHub Actions | GitHub Actions run `26261507979` succeeded on `main` for commit `fbd11e80995fd16037f74d04cec265a3b81889e8` with workflow `CI` | Low | Uncommitted local changes made after that push are outside this run scope | Re-run CI after next push and update run id in context docs |
| Canonical `program_arty_a7_revE.tcl` batch flow is functional and provenance-documented | `program_arty_a7_revE.tcl` now uses explicit whitelist and defaults to skip programming unless `do_program=1`; canonical batch run log `build/logs/phase1_canonical_vivado.log` and verify run log `build/logs/phase1_verify_vivado.log` both close timing (`WNS=0.108`, `WHS=0.039`, `TNS=0.000`, `THS=0.000`) with route checksum `15aef14d5`; provenance captured in `build/BITSTREAM_PROVENANCE.md` | Low | Bitstream equivalence is evidenced by matching route checksum/timing, not external board execution | Keep canonical+verify Tcl runs in batch regression and preserve provenance doc on each new bitstream |
| INT4 quantization accuracy sweep artifact exists for deterministic multi-prompt real-model generation | `firmware/host/run_quantization_accuracy_sweep.py` generates `build/reports/quantization_accuracy_sweep.json` using 20 fixed prompts x 16 generated tokens; current artifact (`generated_at_utc=2026-05-23T19:01:06Z`, `seed=20260523`) reports `aggregate_top1_match_rate=0.534375`; schema lock + floor check in `firmware/host/test_quantization_accuracy_sweep.py` | Low for measurement scaffolding; Medium for cross-environment score portability | Current host lacks `cuda-python`, so INT4 execution backend is `graph_reference_interpreter_fallback` in artifact metadata | Re-run the same script on CUDA-ready host to capture `execution_backend=cuda_graph_executor` while preserving schema and deterministic prompt set |
| RTL perf counters and UART readback opcode exist, simulate, and Vivado timing-closes in simulation-only evidence | `rtl/top/top.sv` adds 64-bit `perf_cycle_counter`, `perf_busy_counter`, `perf_program_count` and UART opcode `MAGIC_READ_PERF=8'hA4` streaming 24 bytes MSB-first; host API `firmware/host/program_loader.py::readPerfCounters()` added; RTL testbench `rtl/tb/tb_perf_counters.sv` passes (`TB_RESULT: PASS`, `PERF_PROGRAM_COUNT=1`); post-change synth/impl in `build/logs/phase3_verify_vivado.log` meets timing (`WNS=0.180`, `WHS=0.023`, `TNS=0`, `THS=0`) with `0 Critical Warnings`, `0 Errors` | Low for implementation + simulation | No on-board capture is staged in-repo; P0 remains open | Keep the wording scoped to simulation + Vivado timing closure until a real board capture artifact is staged |
| Cost-model schedule selection on the 24-shape calibration grid is **bounded-regret with full within-10% coverage** (not "top-1 prediction") | `firmware/host/cost_model.py::select`, `firmware/host/graph_runtime_plan.py` (`RuntimeOpPlan.cuda_schedule` + `cuda_schedule_provenance`); `firmware/host/run_cost_model_selection.py` + `bench/results/cost_model_selection.json`; `firmware/host/test_cost_model_selection.py` (8 tests). Locked artifact (24 shapes × 16 schedules × 10 measured replicates per cell, median per cell as oracle): **mean regret `2.56%`, median `2.51%`, p95 `7.38%`, max `8.04%`, within-5% `87.5%`, within-10% `100%`, mean margin `0.15%`, mean confidence `0.027`**. Top-1 accuracy is `29.2%` and is recorded but explicitly *not* the headline — the supported claim is bounded regret + within-10% coverage. Pruning API (`select_pruned_candidates`) unchanged | Benchmarked | The reason top-1 is low is by design: many candidate schedules are within measurement noise of the oracle on this 4x4 grid, so the model's choice is operationally equivalent to oracle even when it's not byte-equal. Reframing the headline does *not* hide top-1; it just stops promoting a weak signal | Re-run `python firmware/host/run_cost_model_selection.py` after any cost-model refit; do **not** lead public claims with the `29.2%` top-1 — bounded regret (mean 2.56%, max 8.04%, within-10% 100%) is the honest framing |
| The CUDA backend actually **consumes** the cost-model-selected schedule at execution time, not just records it (Phase 7 remediation P2.1+P2.2) | `firmware/host/compiled_runtime.py` (`CompiledMLPRuntime.__init__` takes `schedule_source` parameter with values `"none"` / `"cost_model"` / `"autotuner_cache"` / `"cost_model_then_autotuner"`; legacy `use_tuned_schedule=True` maps to `"autotuner_cache"` for backward compat; `_schedule_params_for_op` resolves the per-op schedule from the chosen source and tags each op trace with `selected_schedule_source=...` so the consumed config is observable). `firmware/host/pytorch_compiler.py::compile_model` exposes the new flag. `firmware/host/test_compiled_runtime_schedule_source.py` (8 tests; in `make test-host` + `.github/workflows/ci.yml`) locks the wiring: `test_schedule_source_cost_model_returns_planned_schedule_for_every_op` fails the build if the executor silently substitutes a different schedule; `test_schedule_source_cost_model_missing_returns_missing_tag` fails it if a coverage gap is silently filled by the autotuner cache when `cost_model` is the requested source. Wall-clock A/B `firmware/host/run_selection_ab.py` + `_selection_ab_cuda_subprocess.py` + `bench/results/selection_ab.json` (24 shapes, warmup=10 + iters=50 + **interleaved A/B per iteration** so GPU clocking is symmetric, bit-exact INT4 output gate on both arms). Live results on WSL2 + RTX 5070 Laptop GPU (sm_120) + Torch 2.11+cu130: **realized regret median `−0.10%`** (cost-model schedule essentially ties oracle on wall-clock), **mean `+0.10%`**, **max `+3.37%`**, **min `−2.39%`**; **within-5% `1.00`**, **within-10% `1.00`** on all 24 shapes; `schedules_identical` (cost model picked the autotuner-oracle exactly) on 7/24 shapes; bit-exact on 24/24. 8 schema tests in `firmware/host/test_selection_ab.py` lock the artifact contract in both `ok` and `cuda_unavailable` modes, including `test_ok_artifact_aggregate_within_5pct_floor` (soft floor at 0.5) and `test_ok_artifact_predicted_link_is_populated_for_every_shape` (artifact must stitch predicted regret to realized regret per shape). Included in `make test-host`, `make repro-host`, `make repro-cuda`, `.github/workflows/ci.yml` | Benchmarked | Realized regret is measured against the autotuner-cache "oracle" which has its own measurement noise (hence the slight negative-regret tail — cost-model schedule occasionally beats the cache); the 24-shape grid is calibration-set only, generalisation on unseen shapes is in the held-out artifact (`cost_model_heldout.json`); no claim about non-blocked-FC ops or non-CUDA backends | Re-run `python firmware/host/run_selection_ab.py --warmup 10 --iters 50` on the same WSL2 + RTX host after any cost-model coefficient refit; do not claim "cost model beats autotuner" — claim that they're wall-clock indistinguishable on this hardware; do not weaken the methodology (drop interleaving, drop sync brackets, drop bit-exactness gate) to engineer a better-looking number |
| Board-fit audit (Phase 7 remediation P3): every realistic blocked-FC workload's lowered program is compared to the instruction-BRAM `PROG_DEPTH` of three reference RTL configurations; replaces the previous silent overflow where every Phase 3 tile reported `fits_instruction_bram=false` against a hard-coded 1024 | `firmware/host/board_config.py` (`BoardConfig` dataclass with frozen factories `pynqz2_baseline` `PROG_DEPTH=1024`, `pynqz2_bram_max` `PROG_DEPTH=8192`, `vu13p_uram` `PROG_DEPTH=131072` + provenance fields `buffer_size`, `array_size`, `data_width_bits`, `notes`); `firmware/host/lowering_blocked_fc_utpu.py::lower_blocked_fc_program_utpu` now takes `prog_depth: int = DEFAULT_PROG_DEPTH=1024` kwarg and reports `fits_instruction_bram = (words <= prog_depth)` + new `instruction_bram_words` field so the host fit-flag matches the board the user is targeting (the RTL parameter `top.sv::PROG_DEPTH` was already parameterised; only the host hard-coded 1024 in 5+ places); `firmware/host/run_board_fit_audit.py` lowers a 14-shape grid (4 tiny demos + 4 Phase 1 calibration shapes + 4 Phase 3 tiling shapes + 2 larger ones) and emits `bench/results/board_fit_audit.json` with per-shape `program_instruction_words` + `fits_per_board` for each board, plus aggregate `shapes_fit_count` + `shapes_fit_fraction` per board. Locked artifact (14 shapes total): `pynqz2_baseline 4/14 (29%)` (only `16x16`, `16x32`, `16x64`, `32x32` fit; this is the smallest-realistic-workload set for the shipping bitstream), `pynqz2_bram_max 8/14 (57%)` (adds `32x64`, `64x64`, `64x128`, `128x128`), `vu13p_uram 12/14 (86%)` (admits everything except `1536x512` and `512x512`). `firmware/host/test_board_fit_audit.py` (7 tests; in `make test-host` + `.github/workflows/ci.yml`) gates: top-level schema; methodology required fields; per-shape layout + monotone-fit-in-prog_depth invariant; reality floors that `(16, 16)` MUST fit the shipping bitstream and `(256, 256)` MUST fit the URAM config (so neither is vapourware) | Benchmarked | The audit reports fit **only**, not execution — bit-exactness vs the NumPy oracle is gated by `tiling_correctness.json` + `test_tiling_controller.py` for the in-bound shapes. The RTL parameter is unchanged in the shipping bitstream; bumping `pynqz2_baseline -> pynqz2_bram_max` requires a synthesis-time PROG_DEPTH override and a fresh bitstream (no host or ISA change). Instruction streaming (the third option from the remediation prompt) is still future work; this audit picks option (2) "larger PROG_DEPTH" + option (3) "board-sized model that already fits", which is the tractable host-only path | Re-run `python firmware/host/run_board_fit_audit.py` after any change to the blocked-FC lowering's instruction count; do **not** claim a workload runs on hardware before it appears in `aggregate.per_board[<board>].shapes_fit` for that board; do not weaken the reality floors in the test (16x16 must fit pynqz2_baseline; 256x256 must fit vu13p_uram) |
| Compiler tiles arbitrary `Linear(M, N)` layers that exceed unified-buffer capacity onto the fixed 512-word buffer via a buffer-capacity-parameterized tiling pass, with bit-identical output to a NumPy int32-accumulated oracle in cycle-accurate ISA simulation (Python ISA simulator) | `firmware/host/tiling_controller.py` (`BufferCapacityModel`, `plan_linear_tiling(out_features, in_features, array_size, buffer_capacity_words, policy)`, `TilingPlan`, `execute_tiled_linear`, `numpy_oracle_int4`); planner derives `max_m_per_tile=1536` for `array_size=16, buffer_capacity_words=512` from the exact `_run_finalize` write pattern; `policy="max_fit_heuristic"` (default) + `policy="cost_model_select"` (wires through Phase 1 `cost_model.select`); `firmware/host/run_tiling_correctness.py` + `bench/results/tiling_correctness.json` (5 workloads, seeds `0x101`/`0x202`/`0x303`/`0x404`/`0x505`); locked artifact: `linear_256x512` 1 tile, `linear_1536x512` 1 tile @ peak=512 words (at boundary), `linear_2048x512` 2 tiles, `linear_4096x768` 3 tiles, `linear_3072x1024` 2 tiles with ReLU — **5/5 bit-identical to oracle, max_abs_err=0 across all 11,008 INT4 outputs**; `firmware/host/test_tiling_controller.py` (24 tests: pure tile-math, partition coverage, schema lock, differential vs oracle on 4 layer shapes, round-trip of fetch-byte decoder, explicit assertion that **un-tiled lowering of M=2048 fails at ISA-encode time** so the tiling test solves a real problem); included in `make test-host` and `.github/workflows/ci.yml` | Benchmarked | Sim-only; no RTL changed; not on-board execution. Phase 1 cost model is calibrated on CUDA blocked-FC, not uTPU tile cycles, so `policy="cost_model_select"` exercises the plumbing end-to-end but its chosen `m_tile` is currently equivalent to the analytical lowest-per-tile-cycle pick; re-calibration on uTPU tile measurements is a Phase 5+ task | Re-run `python firmware/host/run_tiling_correctness.py` after any change to `lowering_blocked_fc_utpu.py` or `isa_simulator.py`; on-FPGA execution claim must wait until upgrade-plan P0 evidence exists |
| RTL is parameter-driven for compute-data-width / accumulator-data-width / array-size / buffer-size / address-size, and a widened current-RTL INT8 instantiation (`ARRAY_SIZE=8`, `BUFFER_SIZE=4096`, `ADDRESS_SIZE=12`) matches a NumPy-validated python ISA simulator oracle byte-for-byte in icarus simulation. For this widened config, the honest status split is: simulated, CI-validated, synthesized at 50 MHz, hardware-executed pending. **Current pipelined-requant synth** (`QUANTIZER_LANES=8`, registered quantizer) is Artix-7 A7-100T `MAX_BATCH_COUNT=4`, post-route `WNS=+2.720 ns`, `LUT=11974`, `FF=6349`, `BRAM tiles=21`, `DSP=72/240` (~64 PE-array + ~8 quantizer), `.bit` generated (`build/reports/requant_pipe_step1_2_mb4_clk20_*`). Combo Step1+2 provenance remains `requant_step1_2_mb4_clk20_*` (`WNS=+0.478 ns`, `DSP=72`). Pre-rightsizing closed_config in `bench/results/baseline_8x8_current_rtl_synth.json` remains provenance (`DSP=192/240`, `WNS=+0.416 ns`). The older `bench/results/p4_2_vivado_reports.json` `100 MHz`, `64 DSP`, `WNS=+1.163 ns` numbers apply to a pre-batching revision and remain provenance only | `rtl/top/top.sv` (derived `ITEMS_IN_SLOT = BUFFER_WORD_SIZE/COMPUTE_DATA_WIDTH`, parameter-driven LOAD_STATE lane unpack, parameterized `QUANTIZER_LANES`/`RELU_LANES`), `bench/results/requant_rightsizing_synth.json`, `bench/results/baseline_8x8_current_rtl_synth.json` (pre-rightsizing provenance) | Verified (synth + simulation) | Hardware-executed still pending. Cite DSP=72 only with the paired finalize-cycle cost in the rightsizing claim row. | Re-synth after `quantizer.sv` / `QUANTIZER_LANES` / top finalize changes; keep baseline JSON as before-number provenance |
| Compiler has a deterministic list scheduler + buffer-slot allocator that orders ISA ops to maximize on-chip buffer reuse for blocked-FC, with bit-exact `fetch_bytes` against the naive lowering and measured cycle/host-store-byte reduction in cycle-accurate Python ISA simulation | `firmware/host/scheduler_allocator.py` (`BufferSlotAllocator` with first-fit-by-finish-time placement + Belady-style spilling + peak-live-words tracking, `BlockedFCScheduler` with input-block hoisting list scheduler, `lower_blocked_fc_program_scheduled` public entry point), `firmware/host/isa_simulator.py` (Phase 5 measurement hooks: `store_bytes_total`, `redundant_store_bytes`, `compute_runs`, `total_macs`, `cycles_per_mac`, `array_utilization` — host-emitted writes only), `firmware/host/run_scheduler_benchmark.py` + `bench/results/scheduler_cycles.json` (8 deterministic shapes from `(16,16)` to `(128,128)` covering `out_blocks` from 1 to 16). Locked artifact: **aggregate sim-cycle reduction `4.67%`** (14400 -> 13728), aggregate host-store-bytes reduction `5.02%` (26792 -> 25448), per-shape cycle savings up to `5.12%` on `(256,64)` (`out_blocks=16, in_blocks=4`); 8/8 shapes report `fetch_bytes_match=true` against the naive baseline; `firmware/host/test_scheduler_allocator.py` (37 tests: allocator first-fit packing / slot-reuse / Belady eviction / oversize rejection / determinism / `base_offset`; scheduler bit-exactness across the 8-shape grid (no-relu and with-relu); byte-deterministic re-runs; cycle-and-store-bytes-never-worse-than-naive gate; exact-savings formula `(ob-1)*ib*(a/items_per_word)*2`; undersized-capacity fall-back to naive; capacity-one-below-full-hoist clean-fall-back; measurement schema lock; layout-reports-input-slot-per-block; JSON-serialisable scheduler metadata) added to `make test-host` and `.github/workflows/ci.yml`. `make repro-host` regenerates the artifact | Benchmarked | Sim-only; cycle counts are simulator cycles (one cycle per ISA op, two-plus-N for BSTORE/BUFFER_XFER), not silicon-measured. RTL FSM cycle cross-check via a scheduled-emission iverilog testbench was previously `TODO/VERIFY`; it is now landed as a separate "scheduler RTL cycle cross-check" row below. Scheduled output deliberately falls back to byte-for-byte naive emission whenever any input block spills, so the 4.67% number is a *lower bound* — buffer-tight workloads regress to naive | Re-run `python firmware/host/run_scheduler_benchmark.py` after any change to `scheduler_allocator.py`, `lowering_blocked_fc_utpu.py`, or `isa_simulator.py`; on-FPGA cycle claim waits on upgrade-plan P0 |
| Scheduler RTL cycle cross-check (Phase 7 remediation P4.1/P4.2): the **percentage** cycle reduction the Phase-5 scheduler advertises in simulation is reproduced by the synthesisable RTL FSM across the board-fit big-MLP sweep, and the scheduler's own bit-exactness invariant (RTL_naive_bytes === RTL_scheduled_bytes) holds at the RTL level for every asserted case | `firmware/host/generate_scheduler_rtl_test_vectors.py` (emits `build/test_vectors/scheduler_naive.mem`, `scheduler_sched.mem`, and `scheduler_expected.svh` with shape-locked simulator cycle counts + per-mille reduction), `firmware/host/run_scheduler_rtl_crosscheck_bigmlp.py` (board-fit sweep over `(32,32)`, `(32,64)`, `(64,32)`, `(64,64)`, `(128,64)`; emits `bench/results/scheduler_rtl_crosscheck_bigmlp.json`), `firmware/host/test_scheduler_rtl_crosscheck_bigmlp.py` (new test lock for the wide artifact), `rtl/tb/tb_scheduler_cycles.sv` (iverilog testbench that streams both programs through `top.sv` with shipping defaults `PROG_DEPTH=1024`, `BUFFER_SIZE=512`, `ARRAY_SIZE=16`, throttles UART pushes on `fifo_in.full` so 6.5k-byte programs survive the 256-byte FIFO, captures cycles between MAGIC_START and HALT_STATE for each program, captures all `tx_we` fetch bytes for both runs, and CHECKs (1) `RTL_sched_cycles < RTL_naive_cycles`, (2) `|RTL_reduction_permille − sim_reduction_permille| ≤ 20 permille (±2.0%)`, (3) `RTL_naive_fetch_bytes === RTL_scheduled_fetch_bytes` per byte); `firmware/host/run_scheduler_rtl_crosscheck.py` remains the single-shape smoke wrapper. Locked artifact for the wide sweep on Windows + iverilog 12.0: all 5 cases pass, `all_cases_ok=true`, `all_cases_rtl_byte_exact=true`, `RTL_naive_bytes === RTL_scheduled_bytes` on every case (`16/16`, `32/32`, or `64/64` fetch bytes depending on shape), and every case meets the `RTL_sched_cycles < RTL_naive_cycles` / `diff_permille <= 20` gate. Promotes the Phase-5 "4.67% sim-only cycle reduction" claim to **RTL-verified at the percentage-reduction level** across the board-fit sweep; the narrow smoke artifact `bench/results/scheduler_rtl_crosscheck.json` is also populated (`status=ok`, RTL 25 permille vs sim 26 permille, diff 1, `scheduler_invariant_holds=true`; regenerated 2026-08-02 on Windows + iverilog). Included in `make test-host`, `make repro-host`, `make sim-iverilog-scheduler-cross-check` (smoke), and `.github/workflows/ci.yml` | RTL-verified | Verifies *percentage* cycle reduction, not absolute cycles — the simulator's 1-cycle-per-op model and the RTL FSM's multi-cycle STORE/FETCH paths give different absolute counts by design. No board-backed FPGA evidence exists yet (P0 open); the single-shape smoke artifact is now `status=ok` with measured RTL values matching the README citation (25 vs 26 permille), and the board-fit sweep remains the primary evidence source for the RTL-level percentage-reduction claim | Re-run `python firmware/host/run_scheduler_rtl_crosscheck_bigmlp.py` on a host with iverilog after any change to `rtl/top/top.sv`, `rtl/unified_buffer/unified_buffer.sv`, `firmware/host/scheduler_allocator.py`, `firmware/host/lowering_blocked_fc_utpu.py`, or `firmware/host/isa_simulator.py`; do **not** restate the cycle savings as a hardware claim — it is `array_size×CLK_PERIOD` simulator-cycle, not wall-clock |
| Cost-model generalizes from calibrated shapes to **unseen** `(in_features, out_features)` shapes with bounded held-out latency MAPE and bounded selection regret on the held-out shapes (Phase 7) | `firmware/host/run_cost_model_heldout.py` (deterministic 80/20 split partitioned by layer shape, SHA-256-seeded with `phase7-heldout-v1`; refits production `_fit_coefficients` on TRAIN rows only; evaluates **latency prediction** and **selection regret** on TEST shapes), `bench/results/cost_model_heldout.json`: **held-out `log_R^2 = 0.926`** (train `0.931`, test/train ratio `0.995`), **held-out `MAPE = 14.32%`** (train `10.86%`, test/train ratio `1.32`), held-out `p95 abs-rel err = 36.38%`; **held-out selection regret mean `5.21%`, median `4.52%`, p95 `10.83%`, max `11.90%`**, within-5% fraction `0.60`, within-10% fraction `0.80`; top-1 = `0.0`; 5/5 held-out shapes have ≥2 distinct schedules tested; `firmware/host/test_cost_model_heldout.py` (9 floor-only tests: schema lock, split-determinism, train/test partition disjoint, `log_R^2_test ≥ 0.85`, `MAPE_test ≤ 25%`, mean-regret `≤ 10%`, within-10% `≥ 0.6`, sorted-key JSON contract; gates leave headroom for modest refit drift). Added to `make test-host`, `make repro-host`, `.github/workflows/ci.yml` | Benchmarked | Top-1 accuracy on held-out is `0.0` — predicted-best schedule is never *exactly* the measured-best on the 5 held-out shapes; the claim is **bounded regret** (≤ 11.90% max, ≤ 10.83% p95), not single-winner prediction. Held-out replay reads the existing CUDA calibration JSON (captured once on WSL2 + RTX 5070 Laptop GPU); a fresh CUDA host re-measurement could shift the numbers, but the gates have margin | Re-run `python firmware/host/run_cost_model_heldout.py` after any change to the cost-model feature basis or the calibration JSON; keep the floor-only gates so refits with new calibration data don't churn CI |
| Phase 7 serious-library baseline harness (v1.2) compares the uTPU autotuner-selected NVRTC blocked-FC kernel against **three** baseline arms on a locked 6-shape `(M, K)` grid: (1) cuBLAS GEMV via `torch.matmul` (INT32 if available, FP32 fallback otherwise — fallback is never silent), (2) **cuBLASLt IMMA INT8 GEMM** via `torch._int_mm` (INT8 inputs / INT32 accumulator — the **dtype-matched** apples-to-apples GEMV comparison; uTPU N=1 padded to N=8 for IMMA alignment with per-N-column reporting), (3) TorchInductor (`torch.compile(nn.Linear, backend="inductor", fullgraph=True)`); populated on WSL2 + RTX 5070 Laptop GPU (sm_120) + Torch 2.11+cu130 with **honest dtype-fallback + IMMA-alignment disclosure** | `firmware/host/run_cublas_baseline.py` (locked `SHAPES = [(16,16),(64,64),(128,128),(256,256),(512,256),(512,512)]`, N=1, default warmup=10 + iters=50, per-shape `mean/median/stdev/min/max/p95` ms summary, INT-MAC TFLOPS where dtype-comparable, gap-vs-baseline % from medians only; v1.2 adds `gap_vs_cublaslt_int8_pct_median` aggregate + `--recompute-aggregate-only` mode), `firmware/host/_cublas_baseline_torch_subprocess.py` (attempts INT32 cuBLAS first → falls back to FP32 cuBLAS GEMV on Torch builds without `addmv_impl_cuda` / `addmm_cuda` int kernels, recording `dtype_fallback_reason` per shape so no silent dtype switch is possible; v1.2 adds `_run_cublaslt_imma_int8_gemm` which times `torch._int_mm(W_i8 (M,K), X_i8 (K,N=8))` with `K % 16 == 0` + `N_padded % 8 == 0` IMMA preflight, INT32 result dtype check, and per-shape `alignment_note` recording both the full-N=8 wall time and `ms_per_n_column_median = kernel_ms.median / N_padded` for the GEMV-equivalent gap; subprocess isolation pattern matches `inductor_oracle_subprocess.py`), `bench/results/cublas_baseline.json` (live, regenerated on WSL2 + RTX 5070, aggregate refreshed in-place 2026-05-26 to surface v1.2 IMMA fields): `status="ok"`, `environment.device_name="NVIDIA GeForce RTX 5070 Laptop GPU"`, `device_capability=(12, 0)`, `torch_version="2.11.0+cu130"`, all 6 shapes ran cuBLAS arm in FP32 fallback with `backend="cublas_gemv_fp32_fallback"` and the same `dtype_fallback_reason` string. **Measured (live 2026-05-26T22:11Z regen on WSL2 + RTX 5070, populated v1.2 IMMA arm)**: cuBLAS-FP32-fallback arm `cublas_gap_pct_median_of_shapes = +4.92%`, mean `+15.41%`, range `[−14.02%, +75.12%]`, all 6 shapes ran. **`inductor_gap_pct_median_of_shapes = −40.50%`** (uTPU faster, dtype caveat applies — uTPU INT8 vs Inductor FP32). **v1.2 IMMA arm (dtype-matched)**: artifact populates `cublaslt_int8_gap_pct_median_of_shapes` (e.g. `+867.38%` on one WSL2 regen across 5 of 6 shapes — uTPU ~10× slower per-N-column vs IMMA). **Any specific gap-% is `[needs-locked-clock-artifact]`** for résumé claims (same baseline-arm clock noise as megakernel latency-%). Qualitative framing: CUDA-core dispatch vs Tensor Core `mma.sync`. The `(M=16, K=16)` shape was IMMA-ineligible: `torch._int_mm` enforces `M > 16` at the Python wrapper layer ("self.size(0) needs to be greater than 16, but got 16"); the preflight catches this case as `imma_unavailable_reason` with the exact wrapper rule named, so the schema-lock test `test_ok_artifact_cublaslt_int8_arm_is_either_populated_or_explicitly_unavailable` treats it as a documented IMMA-unavailability case (NOT a silent drop). **Cross-run noise observation**: the cuBLAS-FP-fallback median moved from `+29.16%` (prior 2026-05-25 run) to `+4.92%` (this 2026-05-26 run) on the same RTX 5070 Laptop GPU with no code change — at GEMV-size timings of 30–50 µs wall-clock, single-machine measurement noise is the dominant signal; the 24-point swing is the noise floor, not a uTPU change. This is exactly reviewer note #4 ("Cross-machine repro for CUDA timings. All latency numbers from one RTX 5070. One sample = not reproducible") firing in real time; a second-host regen (cloud RTX, ~$2) would let us bracket the true cuBLAS gap with a proper across-host estimator. `methodology.cublaslt_int8_alignment_caveat` documents the N=1→N=8 padding + per-column derivation verbatim, and `methodology.baseline_arms` names all three arms explicitly. `firmware/host/test_cublas_baseline.py` (**30 tests, 28 passed + 2 CUDA-only skips on Windows** — includes new `test_methodology_baseline_arms_lists_imma_int8_arm`, `test_methodology_imma_alignment_caveat_is_explicit_about_n_padding`, `test_ok_artifact_cublaslt_int8_arm_is_either_populated_or_explicitly_unavailable` (no silent IMMA misses), `test_ok_artifact_cublaslt_int8_gap_uses_per_column_not_full_n` (gap must be derived from `ms_per_n_column_median`, never from full-N wall time — refuses N_padded× inflation), `test_ok_artifact_aggregate_imma_counts_consistent_with_per_shape` (aggregate counts can never be inflated), `test_torch_subprocess_imma_arm_refuses_misaligned_shapes_without_running` (IMMA preflight rejects K%16≠0 + N%8≠0 before dispatch), `test_recompute_aggregate_in_place_refreshes_methodology_without_inventing_imma_data` (legacy artifact recompute does NOT fabricate IMMA timings)); included in `make test-host`, `make repro-host` (now also calls `--recompute-aggregate-only`), `make repro-cuda`, `make cublas-baseline-recompute-aggregate` (standalone), `.github/workflows/ci.yml` | Benchmarked (all 3 arms populated 2026-05-26) | Split CV gate: strict on `utpu`, advisory on baseline arms (`test_load_bearing_utpu_arm_is_gate_green`). **Do not** quote stable `gap_vs_*_pct` or IMMA gap-% on unlocked WSL2 laptop clocks — `[needs-locked-clock-artifact]` (see `docs/REPRO.md` §7). Qualitative: dtype-matched IMMA row shows uTPU materially slower per-N-column (CUDA cores vs Tensor Cores); FP-fallback row has ≈24-point single-machine noise floor. Inductor row is FP32 vs INT8 (dtype caveat). **NOT a uTPU-beats-cuBLAS claim.** | Re-run `python firmware/host/run_cublas_baseline.py` on WSL2 + RTX 5070; for certifiable gap-%: locked-clock regen per `docs/REPRO.md` §7. Cross-machine repro still recommended for FP-fallback noise bracketing. |
| Generalized producer-consumer fusion engine (Phase 2) is bit/tolerance-equal between fused and unfused on every workload, with measured throughput deltas on **both** NumPy-reference (CPU) and CUDA (RTX 5070, `torch.compile(backend="inductor")` vs eager) paths — **GPU fusion speedup is NOT claimed**: on the locked 3-workload set Inductor compiled-kernel launch overhead dominates the savings from fusion at these tiny shapes | `firmware/host/graph_passes.py` (`FusionEngine` + canonical `DEFAULT_FUSION_RULES`); existing public passes are thin wrappers; `firmware/host/test_graph_passes.py` adds 8 tests (rule-parity, legality predicate veto, multi-consumer rejection, registration-order, wrapper byte-parity). `firmware/host/run_fusion_benchmark.py` extended (Phase 7 remediation) with an optional `cuda_fusion` section: spawns `firmware/host/_fusion_benchmark_cuda_subprocess.py` to time PyTorch eager vs `torch.compile(model, backend="inductor", fullgraph=True)` on the same three workloads, with warmup=5 + iters=30 + `torch.cuda.synchronize` brackets + correctness gate (`rtol/atol=1e-3`). Subprocess isolation (same pattern as `_cublas_baseline_torch_subprocess.py` and `inductor_oracle_subprocess.py`). `bench/results/fusion_payoff.json` has both sections populated on WSL2 + RTX 5070. NumPy section (CPU, host-dependent): `linear_relu_mlp_3x256` `+16.16%` throughput delta (op-count 6→3, bit-identical), `scale_softmax_attention_8x128x128` `-33.50%` (noise-bound), `conv_bn_resnet_block_1x16x32x32` `-1.27%` (conv-dominated). **CUDA section: `linear_relu_mlp_3x256` eager 0.207 ms vs inductor 0.268 ms (`-29.93%`), `scale_softmax_attention_8x128x128` 0.031 ms vs 0.050 ms (`-65.21%`), `conv_bn_resnet_block_1x16x32x32` 0.077 ms vs 0.193 ms (`-151.24%`)**; aggregate `median_throughput_delta_pct = -65.21%`; all 3 workloads correctness-clean (`max_abs_error <= 1.31e-6`). `firmware/host/test_fusion_benchmark.py` adds 3 tests (`test_cuda_fusion_section_is_present_with_locked_keys`, `test_cuda_fusion_status_is_one_of_locked_set`, `test_cuda_fusion_ok_path_covers_every_workload_and_is_correctness_clean`) locking the new schema + skipping the live-only correctness gate on CPU hosts | Benchmarked | NumPy numbers are host-dependent (different CPU + different NumPy build will move them); only the structural op-count reduction is host-independent. The CUDA section explicitly shows that Inductor fusion does **not** win on these tiny workloads on RTX 5070; this is the honest negative result, not an indictment of fusion in general (larger batch / hidden / chain length would change it). FP32-vs-FP32 only on both CUDA paths — no INT8 path is exercised here (that's the cuBLAS baseline harness) | Re-run on a different GPU + different shape regime (e.g. `nn.Linear(2048, 2048)` batch-32) before claiming any GPU fusion speedup; do not generalise a single positive workload into a headline; keep the negative-result wording until a fresh measurement justifies otherwise |
| Metamorphic + differential fuzzer corpus claim is currently `[needs-WSL2-regen]` on this host after stub cleanup | `bench/results/fuzzer_report.json` is currently absent on this host after the CPU/iverilog stub cleanup, so the prior 60k-program / 260k-check WSL2 numbers are not restaged in a traceable local artifact here | Pending | A fresh WSL2 + RTX regen is required before quoting the campaign numbers again from local truth docs | Re-run `python firmware/host/run_fuzzer.py --mode discovery --seed 20260526 --num-graphs 50000 --include-invalid --write-repros` on the WSL2 + RTX host and restage the artifact before restoring stronger wording |
| Equality-saturation superoptimizer (e-graph + 6 semantics-preserving rewrites + bounded saturation + greedy min-cost extraction with `isa_cycle_model` default cost function and `cuda_cost_model` opt-in) now has **two honest corpora**: the original 516-graph random/planted run still shows the load-bearing negative baseline, while a new 8-graph realistic corpus shaped like transformer attention and stacked MLP matmul chains reaches **100.0% win rate**, **87.5% median FLOP reduction on wins**, **93.75% max FLOP reduction on wins**, and **0** extracted wins rejected by the differential-equivalence gate. **`utpu_upgrade_plan.md` Task 3, sim-only, still honest about the random-corpus baseline, but now a real optimization class on model-shaped matmul chains rather than just a toy algebraic rewrite set** | `firmware/host/egraph/` (`egraph.py` ~200 LOC union-find + hashcons + deferred-rebuild congruence closure; `graph_ir_lang.py` shape metadata bridge; `rewrites.py` 6 rules including the load-bearing `SCALE_REASSOCIATION`; `saturate.py` bounded saturation + telemetry; `extract.py` greedy min-cost + FLOP-aware cost), `firmware/host/run_superopt_benchmark.py` (`schema_version=2`; random corpus + realistic corpus; per-graph differential-equivalence verification via `fuzz.differential_oracle.diff_two_graphs`; shape-aware FLOP accounting), `firmware/host/test_egraph.py` (**37 tests** on Windows: FLOP counting, linear-fusion rewrite, batched-matmul association rewrite, float-tolerance acceptance, strict INT rejection, realistic-corpus regression, aggregate derivability, schema lock). Artifact `bench/results/superopt_payoff.json` (Windows host, Python 3.11): top-level random corpus still has `cost_reduction_pct_median=0.0` and `cost_reduction_pct_max=70.58823529411765`; realistic corpus `graphs_evaluated=8`, `win_rate_pct=100.0`, `flop_reduction_pct_median_on_wins=87.5`, `flop_reduction_pct_max_on_wins=93.75`, `rewrites_rejected_by_equivalence_gate=0`. Wired into `make test-host`, `make repro-host`, `make superopt`, `.github/workflows/ci.yml`. | Benchmarked | Sim-only. The `isa_cycle_cost` function is a per-op constant table approximating uTPU ISA cycle weights, NOT an RTL-derived per-program cycle count; the CUDA variant uses a per-op heuristic, not real GPU timing. Keep the random-corpus baseline visible: the 0% median on 516 random/planted graphs is the honest load-bearing baseline and should not be overwritten by the realistic-corpus result. The realistic corpus is intentionally model-shaped (transformer attention `Q.K^T.V` chains and stacked MLP linears), not contrived adversarial chains. `rewrites_rejected_by_equivalence_gate=0` on the realistic corpus is itself a small evidence point: the new matmul reassociation/fusion rules remained sound under differential verification. Do not quote the ISA-cycle numbers as hardware speedups; they are modeled/simulated reductions. | Re-run `python firmware/host/run_superopt_benchmark.py --num-random-graphs 512 --output bench/results/superopt_payoff.json` (or `make superopt`) after any change to `firmware/host/egraph/`, the rewrite set, or the realistic-corpus generator. If the realistic corpus stops being 100% win-rate or the median FLOP reduction drops materially, update the docs and resume wording instead of padding the claim. The documented rule list remains the audit surface |
| Deterministic-latency static-analysis claim is currently `[needs-WSL2-regen]` on this host after stub cleanup | `bench/results/latency_determinism.json` is currently absent on this host after the CPU/iverilog stub cleanup, so the prior local-only latency-determinism artifact is not restaged in a traceable local artifact here | Pending | A fresh WSL2 + RTX / iverilog regen is required before quoting the prior latency-determinism numbers again from local truth docs | Re-run `python firmware/host/run_latency_determinism.py` on the WSL2 + RTX / iverilog host and restage the artifact before restoring stronger wording |
| Fused CUDA region-kernel codegen + benchmark (`utpu_upgrade_plan.md` Task 1 v1.1) — IR-level region planner ACTIVELY rejects multi-Linear chains as `global_sync_required`; legal v1 regions are `linear_with_epilogue` and `elementwise_chain`. CUDA codegen emits one NVRTC kernel per region. **Load-bearing resume claims (noise-immune, WSL2 + RTX 5070, v2.1 timing protocol)**: **`launch_reduction_vs_op_by_op_pct_pooled=62.5%`** (6 fused / 16 op_by_op across six workloads; 66.7% on four launch-bound chains ≥3), **`all_workloads_correct=true`**, split CV gate strict on `fused_region` (`test_load_bearing_fused_region_arm_is_gate_green_megakernel`). **`latency_reduction_vs_op_by_op_pct_median` is `[needs-locked-clock-artifact]`** — same workloads produced 35%→13%→37%→58% under successive trim widths (clock noise in `op_by_op` denominator, not convergence). v1.1: `cuda_graphs_op_by_op` 4th arm, arithmetic-bound workload placeholders, launch-count aggregate derivable from integer `kernel_launches_per_invocation`. **NOT a cuBLAS-beating claim**; **NOT a stable latency-% claim** on unlocked laptop clocks. | `firmware/host/region_fusion.py` (legality + global-sync-trap rejection; 14 tests in `test_region_fusion.py`), `firmware/host/cuda_megakernel_backend.py` (NVRTC codegen for both region kinds + per-op codegen for op_by_op arm), `firmware/host/_megakernel_cuda_subprocess.py` (4-arm timing in isolated subprocess: `fused_region`, `op_by_op`, `cuda_graphs_op_by_op` (v1.1), `cublas_fp32`; all three NVRTC arms compile kernels ONCE + pre-allocate buffers ONCE outside the timed loop so the comparison measures only `cuLaunchKernel`/`cuGraphLaunch`), `firmware/host/run_megakernel_benchmark.py` (workload spec + aggregate; v1.1 adds `--recompute-aggregate-only` mode to re-derive aggregate from existing per-arm timings without re-running CUDA), `firmware/host/test_megakernel_benchmark.py` (**21 always-on tests**: codegen smoke + schema lock + ok-mode strict gates `primary_arms_must_actually_succeed`, `aggregate_consistent_with_arms`, `fused_region_not_catastrophically_slow`, `launch_reduction_is_real_and_consistent`, `cuda_graphs_arm_present_or_legacy_artifact`, `cuda_megakernel_diff_oracle_runner_skips_cleanly_without_cuda` + new `pending_regen` placeholder handling for v1.1 workloads added after the parent artifact was committed). Locked artifact `bench/results/megakernel_payoff.json` (v2.1 protocol; WSL2 + RTX 5070): `launch_reduction_vs_op_by_op_pct_pooled=62.5%`, `pooled_fused_launches=6`, `pooled_op_by_op_launches=16`, `all_workloads_correct=true`, split-CV fields `max_timing_stability_pct_load_bearing_arm` / `max_timing_stability_pct_baseline_arms`; `latency_reduction_vs_op_by_op_pct_median` diagnostic only (`[needs-locked-clock-artifact]` for claims). Tests include `test_load_bearing_fused_region_arm_is_gate_green_megakernel`, `test_baseline_arms_advisory_cv_reporting_megakernel`. Wired into `make test-host`, `make repro-host`, `make repro-cuda`, `make megakernel-recompute-aggregate`, CI. | Benchmarked | **Do not** quote `latency_reduction_vs_op_by_op_pct_median` on the résumé until locked-clock regen (`nvidia-smi --lock-gpu-clocks` on native Linux or cloud RTX). Passing the strict split CV gate does **not** certify the latency ratio. Launch-count + bit-exact are the only noise-immune headline fields. | Re-run `python firmware/host/run_megakernel_benchmark.py` on WSL2 + RTX 5070 (v2.1 defaults). For a certifiable latency-%: locked-clock regen per `docs/REPRO.md` §7. |
| Nsight Compute occupancy/bottleneck-profile claim is currently `[needs-WSL2-regen]` on this host after stub cleanup | `bench/results/nsight_compute_profile.json` is currently absent on this host after the CPU/iverilog stub cleanup, so the prior stub-backed nsight row is not restaged in a traceable local artifact here | Pending | A fresh WSL2 + RTX + `ncu` regen is required before quoting nsight-backed status again from local truth docs | Re-run `python firmware/host/run_nsight_compute_profile.py` on the WSL2 + RTX host with `ncu` installed and restage the artifact before restoring stronger wording |
| Project has a publication-quality end-to-end writeup with measured numbers only, embedded artifact links, explicit dtype/scope caveats, and a one-command repro that regenerates every headline (Phase 7) | `docs/WRITEUP.md` (full-stack narrative: FX → Graph IR → passes → cost-model selection + held-out generalization → CUDA NVRTC + uTPU ISA → scheduler/allocator → multi-PE sim → parameter-driven INT8 RTL → cuBLAS / Inductor baseline methodology; every quoted number matches an artifact in `bench/results/`); `docs/REPRO.md` (one-command repro map: `make repro` host-only artifacts, `make sim-iverilog-all` RTL bitmatch, `make repro-cuda` CUDA artifacts; §6 claim→command→artifact table); `Makefile` `repro` / `repro-host` / `repro-cuda` targets cover every artifact referenced in the writeup; `.github/workflows/ci.yml` regenerates fusion-payoff, tiling-correctness, scheduler-cycles, cost-model-heldout, and cuBLAS-baseline-stub artifacts before running `make test-host` so they cannot drift from the test gates | Verified (docs) | Writeup-level claim — depends on every artifact-row claim above remaining accurate. The `cuBLAS gap` value is intentionally left bracketed in the writeup until the GPU-host artifact populates with `status="ok"` | Re-read `docs/WRITEUP.md` against `bench/results/*.json` after any artifact changes; the gates in `test_cublas_baseline.py` + `test_cost_model_heldout.py` + `test_scheduler_allocator.py` + `test_tiling_controller.py` + `test_fusion_benchmark.py` + `test_cost_model_regression.py` + `test_cost_model_selection.py` + `test_multi_pe_sim.py` will fail the build if any locked number changes |
| Lowered a transformer attention block's GEMMs (Q/K/V projections, QK^T, AV, output projection) to the accelerator's dynamic-operand matmul datapath, bit-exact vs an integer reference and RTL on representative `qk` / `av` shapes; softmax / LayerNorm / residual run on the host | `bench/results/utpu_attention_hybrid.json`, `firmware/host/utpu_batched_matmul_lowering.py`, `firmware/host/test_utpu_batched_matmul_lowering.py`; artifact records `host_accelerator_boundary.accelerator_ops=["q_proj","k_proj","v_proj","qk","av","out_proj"]`, `host_accelerator_boundary.host_ops=["norm","view/permute reshapes","scaled_softmax","residual_add"]`, `correctness_gate.packed_accelerator_outputs_bit_exact_vs_integer_reference=true`, `backend_parity.all_bit_exact=true`, `backend_parity.backends_skipped=[]`, `rtl_bitmatch.qk_shape.rtl_sim_passed=true`, `rtl_bitmatch.av_shape.rtl_sim_passed=true`, and `cuda_backend_execution_device="cpu"` because Torch CUDA on this host lacks the required INT32 `addmm` path | Verified (simulation) | Hybrid only. Do not claim "runs attention" or "runs a transformer block on the accelerator." Softmax / norm / residual remain on host and that boundary must stay explicit. The parity `cuda` backend ran on CPU on this host and is not a GPU claim. Identity requant here is for lowering correctness, not accuracy. | Re-run `python firmware/host/run_utpu_attention_hybrid.py` and `python -m pytest firmware/host/test_utpu_batched_matmul_lowering.py -q` after any change to dynamic `BATCHED_MATMUL` lowering, the blocked-FC ISA path, or the hybrid attention harness |
| Implemented per-output-channel symmetric requantization across the ISA, simulator, and RTL, bit-exact ISAâ†”RTL through `quantizer_array` with distinct per-lane scales and no-X on the finalize path | `bench/results/real_model_accelerator.json`, `firmware/host/test_blocked_fc_requant.py`; artifact `accuracy_comparison.int8.per_layer_accuracy=0.9733`, `accuracy_comparison.int8.per_channel_accuracy=0.9728`, `accuracy_comparison.int4.per_layer_accuracy=0.5832`, `accuracy_comparison.int4.per_channel_accuracy=0.6536`, `accelerator_validation.bit_exact_vs_reference=true`, `accelerator_validation.isa_rtl_bitmatch=true`; focused test coverage drives distinct per-lane multipliers / shifts through `quantizer_array` and checks no-X on finalize outputs | Verified (simulation) | This is a co-design capability, not an accuracy win. INT8 stayed flat (`0.9733` vs `0.9728`, within noise of float `0.9730`), and while INT4 improved (`0.5832 â†’ 0.6536`), it remains weak. Do not claim per-channel improved model accuracy. | Re-run `python firmware/host/run_real_model_accelerator.py` and `python -m pytest firmware/host/test_blocked_fc_requant.py firmware/host/test_real_model_accelerator.py -q` after any change to requant parameter encoding, finalize semantics, or `quantizer_array` |
| Validated the full hostâ†’deviceâ†’host path in simulation: a lowered 8x8 INT8 program is uploaded over a real serial UART path into the RTL, executed, and its UART-captured output is byte-exact to the cycle-accurate ISA simulator; no-X on the finalize and TX paths | `bench/results/uart_preboard_roundtrip.json`, `firmware/host/uart_replay.py`, `rtl/tb/tb_uart_replay.sv`, `docs/evidence/fpga_onboard/`; artifact `demo_program.name="int8_fc8x8_single_tile_uart_replay"`, `demo_program.program_words=136`, `demo_program.prog_depth=256`, `demo_program.fits_prog_depth=true`, `uart_roundtrip.captured_uart_matches_isa_sim=true`, `rtl_simulation.no_x_quantizer_finalize=true`, `rtl_simulation.no_x_uart_tx_path=true`, `on_silicon.status="simulation"` | Verified (simulation) | Simulation only. This is I/O-path validation in iverilog, not a board / FPGA / silicon run. The byte-match is captured-UART-vs-ISA-sim for the same lowered program, not sim-vs-sim. `on_silicon.status` must remain `simulation` until a physical run exists. | Re-run `python firmware/host/run_uart_preboard_demo.py` and `python -m pytest firmware/host/test_uart_replay.py -q` after any change to the UART upload protocol, top-level UART wiring, or replay testbench |
| Right-sized the requant/ReLU finalize datapath for Artix-7 DSP budget (**8x8 result, not a 16x16 enabler**): Step1 saturates the requant multiply operand to signed 25-bit (1 DSP/quantizer); Step2 defaults `QUANTIZER_LANES=ARRAY_SIZE` with column-streamed finalize; Step2b optional registered quantizer (`QUANTIZER_PIPE_DEPTH`, default **0=combo** until Fmax proven). Measured closed_config `ARRAY_SIZE=8` INT8 @ 50 MHz: baseline **DSP 192/240**, `WNS=+0.416 ns` → Step1-only (`QUANTIZER_LANES=64`) **DSP 128/240**, `WNS=+0.275 ns` → Step1+2 combo (`QUANTIZER_LANES=8`) **DSP 72/240**, `WNS=+0.478 ns` → Step2b `PIPE_DEPTH=1` **DSP 72/240**, `WNS=+2.720 ns` (data-path 16.983 ns; period−WNS≈17.28 ns ⇒ ~57.9 MHz, **not** headroom to 100 MHz). Step2b critical path was `quantizer_in_reg → DSP A→P (MREG=0) → combo shift/clamp → result_reg`. `PIPE_DEPTH=3` (product/shift/clamp) Fmax sweep (`bench/results/timing_closure_sweep.json`, mb4): **closes 20→10 ns**; highest clean close **100 MHz** (`clk10`, `WNS=+0.419 ns`, data-path 8.822 ns); binding path at 100 MHz is `compute_load_en_reg → PE partial_sum_out CE` (load/compute mutual exclusion), not requant. Quantizer DSP at depth=3 shows **PREG=1, MREG=0**. Finalize wait_clear A/B (DEPTH=1): `+0/+6/+30/+60` at B=1/4/16/32. | `bench/results/requant_rightsizing_synth.json`, `bench/results/timing_closure_sweep.json`, Vivado `requant_fmax_mb4_clk*_pd3_*`, `firmware/host/test_blocked_fc_requant.py` | Verified (synth + simulation) | Default remains `QUANTIZER_PIPE_DEPTH=0` until product decision to ship depth=3. mb16/mb64 sweep still filling. Not on-silicon. | Prefer depth=3 for Fmax; next lever is weight double-buffer FSM overlap (PE shadow already landed) |
| **INT8 MAC packing is dead on Artix-7 / DSP48E1 (documented negative result — do not retry).** OOC synth (`scripts/synth_packed_mac_ooc.tcl`): `pe_packed_skewed` = **2 DSP/cell** (cascade `A*B` + `PCIN>>17`), packed 8x8 = **64 DSP** (identical to unpacked INT8 MAC), packed 16x16 = **240/240** (would need ~256). Primitive cause: DSP48E1 A-port is 25-bit; INT8xINT8 lane packing needs `PACK_SHIFT>=17` so the packed A operand spills a second DSP. Xilinx INT8 packing targets DSP48E2 (27-bit A, UltraScale+), not 7-series. **ARRAY_SIZE=16 INT8 is permanently infeasible on `xc7a100tcsg324-1`:** 256 unpacked MAC DSPs alone exceed 240 before requant. Intermediate sizes 10/12/14 fail `unified_buffer` integrality: `BANKS=N^2/ITEMS_IN_SLOT` must divide `BUFFER_SIZE` (e.g. N=12 INT8 → BANKS=72 does not divide 4096). Stop INT8 packing; spare DSPs go to Fmax/occupancy at 8x8, not width. | `bench/results/packed_dsp_synth.json`, `bench/results/requant_rightsizing_synth.json` (`task_b`, `task_c_decision_gate`), `rtl/PEArray/pe_array_packed.sv`, `rtl/unified_buffer/unified_buffer.sv` | Verified (synth finding) | Do not revive INT8 packing on this part. | Never cite the old 128-DSP INT8 packed estimate |
| **INT4 MAC packing OOC probe (Pareto point only):** `pe_packed_pair` with `COMPUTE_DATA_WIDTH=4`, `PACK_SHIFT=9` measures **1 DSP/pair** on DSP48E1 (mode `(D'+A2)*B`); INT8 cell recheck still **2 DSP/pair**. Implies ~128 DSP for 16x16 INT4 MACs (fits 240 before requant). Accuracy context from `real_model_accelerator.json`: INT4 **58.32%** vs INT8 **97.33%**. Not a path back to useful 16x16 INT8 workloads. | `bench/results/packed_dsp_synth.json` (`int4_packing_probe`), `build/reports/packed_mac_ooc_int4_cell_utilization.rpt`, `bench/results/real_model_accelerator.json` | Verified (OOC synth) | Stop after DSP-per-pair report unless explicitly pursuing the INT4 Pareto design point. | Do not build 16x16 INT4 arrays without a separate accuracy/throughput decision |
