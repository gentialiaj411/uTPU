- Built a retargetable blocked-FC compiler path that shares problem modeling/scheduling across uTPU and CUDA and diverges only at backend lowering/runtime dispatch, measured by one-command reproducibility (`make bench`) and five-run locked reports with raw JSON artifacts in `benchmarks/run_01..run_05`.
- Reduced uTPU fused inference program footprint by fusing FC1→FC2 into a single compressed program, measured by stable BRAM usage of 1017 words (min/median/max all 1017 over 5 runs), fitting within the 1024-word instruction BRAM budget while preserving block-runtime correctness (90.0% accuracy, max abs logit diff 0.0 vs legacy on 100 samples).
- Implemented and benchmarked a CUDA blocked-FC backend with NVRTC+driver execution and cuBLAS comparison, measured by representative-MLP shape (M=64,K=256) kernel latency 0.1019 ms median, transfer overhead 88.9203% median, and kernel speed 86.1927% median vs cuBLAS across 5 locked runs.
